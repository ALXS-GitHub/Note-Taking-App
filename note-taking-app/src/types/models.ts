export type NoteInfo = {
    id: string;
    title: string;
    content: string;
    lastModified: string;
    isActive: boolean;
    isPinned: boolean; // for later use
    color: string; // for later use
}